package org.firstinspires.ftc.teamcode.utilities.opmodes;
/*
import com.qualcomm.robotcore.eventloop.opmode.OpMode;

import org.firstinspires.ftc.teamcode.utilities.drivetrain.Drivetrain;
import org.firstinspires.ftc.teamcode.utilities.hardware.HardwareGetter;
import org.firstinspires.ftc.teamcode.utilities.manipulators.Arm;
import org.firstinspires.ftc.teamcode.utilities.manipulators.DuckSpinner;
import org.firstinspires.ftc.teamcode.utilities.manipulators.Intake;

public class TeleopOpMode extends OpMode {
    HardwareGetter hardwareGetter;

    Drivetrain drivetrain;
    DuckSpinner duckSpinner;
    Arm arm;
    Intake intake;

    @Override
    public void init() {
        hardwareGetter = new HardwareGetter(hardwareMap);

        drivetrain = new Drivetrain();
        drivetrain.frontLeftMotor = hardwareGetter.frontLeft;
        drivetrain.frontRightMotor = hardwareGetter.frontRight;
        drivetrain.backLeftMotor = hardwareGetter.backLeft;
        drivetrain.backRightMotor = hardwareGetter.backRight;
        drivetrain.gamepad1 = gamepad1;
        drivetrain.gamepad2 = gamepad2;
        drivetrain.Initialize();

        duckSpinner = new DuckSpinner();
        duckSpinner.spinnerMotor = hardwareGetter.spinnerMotor;
        duckSpinner.gamepad1 = gamepad1;
        duckSpinner.gamepad2 = gamepad2;
        duckSpinner.Initialize();

        arm = new Arm();
        arm.Initialize();

        intake = new Intake();
        intake.Initialize();

    }

    @Override
    public void start() {

    }

    @Override
    public void loop() {

    }

    @Override
    public void stop() {

    }
}*/